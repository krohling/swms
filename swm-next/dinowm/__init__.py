"""DinoWM-architecture world model, trained and evaluated under the SWM
reproduction recipe (see train_predictor.py). Model code ported from
krohling/dino_wm (branch qwen3-vl-encoder); everything else — dataset,
sampling, schedule, evaluation — comes from the reproduction."""
